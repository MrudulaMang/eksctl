apiVersion: v1
kind: Service
metadata:
    name: loadbalancer
spec:
    type: LoadBalancer
    selector: 
        project: roboshop
        component: frontend
        environment: dev
    ports:
        - protocol: TCP
          port: 80 #service port
          targertPort: 80 #container port
          nodePort: 32141
            