apiVerison: v1
kind: Service
metadata:
  name: nginx
spec:
  selector: #this service should select which pods
    #labels of the pods which u want to atach , here i gave labels.yaml labels
    project: roboshop
    component: frontend
    environment: dev
  ports:
    protocol: TCP
    port: 80 #service
    targetPort: 80 #container 